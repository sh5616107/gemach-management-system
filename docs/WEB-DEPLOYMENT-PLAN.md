# תכנית העלאה לאינטרנט - Web Deployment

## מצב נוכחי ✅

- Backend API מלא רץ על Railway
- Database: Neon PostgreSQL
- Frontend: עובד לוקלית עם JSON
- Login API: עובד

## מטרה 🎯

להעלות את הFrontend לאינטרנט ולחבר אותו ל-Backend.

---

## אסטרטגיה

### גישה 1: Build ו-Deploy כמו שזה (מומלץ!)

**הרעיון:** להעלות את הFrontend הנוכחי ל-Vercel עם LocalStorage.

**יתרונות:**
- מהיר - 10 דקות
- עובד מיד
- אין צורך בשינויי קוד
- כל משתמש יש לו נתונים משלו (LocalStorage)

**חסרונות:**
- נתונים לא משותפים בין משתמשים
- נתונים נשמרים בדפדפן בלבד

---

### גישה 2: חיבור מלא ל-API

**הרעיון:** לשנות את הקוד להשתמש ב-API במקום LocalStorage.

**יתרונות:**
- נתונים משותפים
- נתונים מאובטחים בשרת
- גישה מכל מקום

**חסרונות:**
- דורש שינויי קוד רבים
- צריך לתקן async/await
- יותר זמן

---

## ההחלטה: גישה 1 עכשיו, גישה 2 בהמשך

1. **עכשיו:** נעלה את הFrontend כמו שהוא
2. **אחר כך:** נשפר בהדרגה

---

## שלבי ביצוע

### 1. הכנת הפרויקט ל-Vercel

```bash
npm run build
```

### 2. יצירת vercel.json

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite"
}
```

### 3. העלאה ל-Vercel

- התחבר ל-Vercel
- Import הפרויקט מ-GitHub
- Deploy!

### 4. הגדרת משתני סביבה ב-Vercel

```
VITE_API_URL=https://gemach-management-system-production.up.railway.app
VITE_USE_API=false
```

---

## תוצאה צפויה

- ✅ אתר חי באינטרנט
- ✅ עובד עם LocalStorage
- ✅ Backend מוכן לעתיד
- ✅ Electron ממשיך לעבוד

---

**תאריך:** ${new Date().toLocaleDateString('he-IL')}
