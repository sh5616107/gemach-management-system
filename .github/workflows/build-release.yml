name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., v2.1.0)'
        required: true
        default: 'v2.9.58'

permissions:
  contents: write
  actions: read
  checks: write

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Clear npm cache
      run: |
        npm cache clean --force
        if (Test-Path node_modules) { Remove-Item -Recurse -Force node_modules }
        if (Test-Path package-lock.json) { Remove-Item -Force package-lock.json }
        
    - name: Install dependencies
      run: npm install --no-audit --no-fund
      
    - name: Verify @hebcal/core installation
      run: npm list @hebcal/core
      
    - name: Build Electron app
      run: npm run dist
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true
        
    - name: List built files
      run: |
        echo "Listing dist-electron directory:"
        dir dist-electron
        
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: מערכת-ניהול-גמח-windows
        path: |
          dist-electron/*Setup*.exe
          
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ github.event.inputs.version || github.ref_name }}
        name: מערכת ניהול גמ"ח ${{ github.event.inputs.version || github.ref_name }}
        body: |
          ## 🏛️ מערכת ניהול גמ"ח - גרסה ${{ github.event.inputs.version || github.ref_name }}
          
          ### 🔥 תיקון קריטי בגרסה 2.9.58:
          - � נ**תיקון איבוד נתונים** - הנתונים נשמרים עכשיו לקובץ בדיסק
          - 🔐 **תיקון לוגין** - שימוש בלוגין המקומי במקום לוגין הווב
          - 🔄 **גיבוי כפול** - שמירה גם ב-localStorage וגם בקובץ
          
          ### ✨ תכונות עיקריות:
          - 💰 ניהול הלוואות מתקדם עם מעקב פרעונות
          - � ניהו ל פקדונות ומשיכות
          - 🎁 ניהול תרומות עם הדפסת קבלות
          - 🖨️ הדפסה משופרת
          - 📁 שמירת PDF
          - 🎨 ממשק בעברית
          - 📊 דוחות וסטטיסטיקות
          - 🛠️ כלים מנהליים - ניהול ערבים, רשימה שחורה ומכתבי התראה
          - 🔄 הלוואות מחזוריות ופרעונות אוטומטיים
          - � תאריכיםם עבריים
          
          ### � הוראהות התקנה:
          1. הורד את קובץ ה-Setup.exe
          2. הפעל את הקובץ והתקן
          3. פתח את האפליקציה
          4. ייבא נתונים מגיבוי (אם יש)
          
          ---
          **פותח עבור קהילת הגמ"חים בישראל 🇮🇱**
        files: |
          dist-electron/gemach-management-system-setup-*.exe
        draft: false
        prerelease: false
        fail_on_unmatched_files: false
        generate_release_notes: false
        make_latest: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
