name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., v2.1.0)'
        required: true
        default: 'v2.9.57'

permissions:
  contents: write
  actions: read
  checks: write

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Clear npm cache
      run: |
        npm cache clean --force
        if (Test-Path node_modules) { Remove-Item -Recurse -Force node_modules }
        if (Test-Path package-lock.json) { Remove-Item -Force package-lock.json }
        
    - name: Install dependencies
      run: npm install --no-audit --no-fund
      
    - name: Verify @hebcal/core installation
      run: npm list @hebcal/core
      
    - name: Build React app
      run: npm run build
      
    - name: Build Electron app
      run: npm run build-electron
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true
        
    - name: List built files
      run: |
        echo "Listing dist-electron directory:"
        dir dist-electron
        
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: מערכת-ניהול-גמח-windows
        path: |
          dist-electron/*Setup*.exe
          
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ github.event.inputs.version || github.ref_name }}
        name: מערכת ניהול גמ"ח ${{ github.event.inputs.version || github.ref_name }}
        body: |
          ## 🏛️ מערכת ניהול גמ"ח - גרסה ${{ github.event.inputs.version || github.ref_name }}
          
          ### ✨ תכונות עיקריות:
          - 💰 ניהול הלוואות מתקדם עם מעקב פרעונות
          - 🏦 ניהול פקדונות ומשיכות
          - 🎁 ניהול תרומות עם הדפסת קבלות
          - 🖨️ הדפסה משופרת עם פתרון בעיית Windows
          - 📁 שמירת PDF (ב-Electron)
          - ⚙️ פונקציות מתקדמות אופציונליות
          - 🎨 ממשק בעברית עם ערכות נושא
          - 📊 דוחות וסטטיסטיקות
          
          ### 🆕 חדש בגרסה 2.9.40:
          - � **טעלינה מהירה ללא הבהובים** - פתרון מלא לבעיית ההבהוב בפתיחת האפליקציה
          - 🎨 **מסך טעינה מקצועי** - splash screen יפה עם אנימציות חלקות
          - ⚡ **אופטימיזציות ביצועים** - טעינה מהירה יותר עם preload script מתקדם
          - � **שכיפורי בדיקת עדכונים** - ממשק מתקדם עם תצוגה מקדימה של התהליך
          - 🛠️ **כלים מנהליים חדשים** - ניהול ערבים, רשימה שחורה ומכתבי התראה
          - � ***ניהול ערבים מתקדם** - הוספה, עריכה ומחיקה של ערבים עם רשימה מוכנה
          - 🚫 **רשימה שחורה** - ניהול לווים חסומים עם התראות אוטומטיות
          - � **מכתבי מהתראה** - יצירת מכתבי התראה מקצועיים ללווים
          - 🏦 **בחירת סניף בנק** - הוספת פרטי סניף בנק למסמכים
          - 📊 **דוחות משופרים** - עיצוב מקצועי יותר עם לוגו וסניף בנק
          
          ### 🔧 הוראות התקנה:
          1. הורד את קובץ ה-Setup.exe
          2. הפעל את הקובץ והתקן
          3. פתח את האפליקציה
          4. התחל להזין נתונים
          
          ### 📚 תיעוד מלא:
          ראה את ה-README בפרויקט למדריך שימוש מפורט.
          
          ---
          **פותח עבור קהילת הגמ"חים בישראל 🇮🇱**
        files: |
          dist-electron/gemach-management-system-setup-*.exe
        draft: false
        prerelease: false
        fail_on_unmatched_files: false
        generate_release_notes: false
        make_latest: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
