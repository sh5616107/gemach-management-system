# Electron Environment Variables
# This file is used when building Electron app (npm run build)

# IMPORTANT: Use LocalStorage for Electron (offline mode)
VITE_USE_API=false

# App Configuration
VITE_APP_NAME=מערכת ניהול גמ"ח
VITE_APP_VERSION=2.9.58
